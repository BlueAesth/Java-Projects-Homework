
package janoshcontactdirectory;

/**
 *
 * @author Poten
 */
public class JanoshContactDirectory 
{

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) 
    {
        /*
        directory has linked list of contact info (Has add method delete, etc) directory can extend contact info
        Main- just needs to create contact directory
        File manager Save and load need file name, save needs obj that its gunna save, load is gunna need obj that its going to returning we cast to the point 
        iterator example for file manager?
        write object, either give param that isnt specific to contact dsirectory, or make file manager a template<T>
        work with any serialized class
        
        update- update needs to know, search for last name or something like that. find the thing in the list, okay to prompy user to change, 
        */
    }
    
}
